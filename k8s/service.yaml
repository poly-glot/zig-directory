apiVersion: v1
kind: Service
metadata:
  name: dmozdb
  namespace: dmozdb
  labels:
    app: dmozdb
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: dmozdb
