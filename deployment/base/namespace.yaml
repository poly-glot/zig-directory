apiVersion: v1
kind: Namespace
metadata:
  name: dmozdb
  labels:
    app: dmozdb
    # The ClusterSecret operator injects the OCIR image-pull secret
    # (ocirsecret) into every namespace carrying this label.
    registry: "oci"
